apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-reviews-data
  labels:
    app.kubernetes.io/name: postgres-reviews
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: eks-auto-ebs
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-reviews
  labels:
    app.kubernetes.io/name: postgres-reviews
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres-reviews
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres-reviews
    spec:
      securityContext:
        fsGroup: 70
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: bookstore
            - name: POSTGRES_PASSWORD
              value: bookstore
            - name: POSTGRES_DB
              value: bookstore_reviews
            - name: POSTGRES_HOST_AUTH_METHOD
              value: trust
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: pgdata
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - bookstore
                - -d
                - bookstore_reviews
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-reviews-data
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-reviews
  labels:
    app.kubernetes.io/name: postgres-reviews
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres-reviews
  ports:
    - port: 5432
      targetPort: 5432
