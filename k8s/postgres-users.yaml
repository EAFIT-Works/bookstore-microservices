apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-users-data
  labels:
    app.kubernetes.io/name: postgres-users
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: eks-auto-ebs
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-users
  labels:
    app.kubernetes.io/name: postgres-users
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres-users
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres-users
    spec:
      securityContext:
        fsGroup: 70
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: bookstore
            - name: POSTGRES_PASSWORD
              value: bookstore
            - name: POSTGRES_DB
              value: bookstore_users
            - name: POSTGRES_HOST_AUTH_METHOD
              value: trust
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: pgdata
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - bookstore
                - -d
                - bookstore_users
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-users-data
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-users
  labels:
    app.kubernetes.io/name: postgres-users
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres-users
  ports:
    - port: 5432
      targetPort: 5432
